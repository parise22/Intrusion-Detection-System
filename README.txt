Intrusion Detection System

To compile:
- Place all added files in the same directory.
- Run the following command to compile 
	CC main.cpp function.cpp -o IDSE

To run:
- Run the following command to run
	./IDSE Events.txt Base-Data.txt Test-Events.txt
- The naming of the files is not important, however the order must be followed.
